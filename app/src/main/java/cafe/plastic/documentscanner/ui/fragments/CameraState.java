package cafe.plastic.documentscanner.ui.fragments;

public class CameraState {
        public enum Flash {
            OFF,
            ON,
            AUTO
        }

        public enum Outline {
            ON,
            OFF
        }
    }
